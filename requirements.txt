# requirements.txt - Optimizado para producción
# NOTA: PyTorch y torchvision se instalan en Dockerfile con CPU-only
# NOTA: ultralytics se instala en Dockerfile sin dependencias automáticas

nats-py==2.6.0
python-dotenv==1.0.0
opencv-python-headless  # Solo esta versión, NO opencv-python
numpy
webcolors
asyncpg>=0.29.0

# Dependencias mínimas de ultralytics (sin torch, sin opencv duplicado)
pyyaml
requests
scipy
tqdm
pillow