# # docker/Dockerfile (sin USAR)
# FROM python:3.9

# WORKDIR /app

# # Instalar dependencias del sistema para OpenCV
# RUN apt-get update && apt-get install -y \
#     libglib2.0-0 \
#     libsm6 \
#     libxext6 \
#     libxrender-dev \
#     libgomp1 \
#     libglib2.0-0 \
#     libgl1-mesa-glx \
#     && rm -rf /var/lib/apt/lists/*

# # Copiar requirements
# COPY requirements.txt .

# # Instalar dependencias de Python
# RUN pip install --no-cache-dir -r requirements.txt

# # Copiar código fuente
# COPY . .

# # Exponer puerto (opcional, NATS no requiere puerto específico)
# EXPOSE 8080

# # Comando por defecto
# CMD ["python", "main.py"]
